namespace RESTSchemaRetry.Enum
{
    /// <summary>
    /// The delay types for the retry schema
    /// </summary>
    public enum DelayTypes
    {
        LINEAR,
        EXPONENTIAL
    }
}
